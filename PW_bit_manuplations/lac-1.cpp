#include<iostream>
using namespace std;
int main(){
    //What is the difference between the bit wise and the logical operator?
    //Bit wise operator;
    // these are applyed on the corrosoponding bit of the number:
    //And in the logical operator we work on the 2 bool input:
    //Example of the logical operator:- &&,|| etc....
    return 0;
}